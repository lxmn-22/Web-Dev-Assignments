<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note Taking Express App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-700">
        <div class="form p-10 text-white ">
            <form class="flex flex-col items-center" action="/create" method="post">
                <input class="w-1/3 block px-5 py-2 bg-zinc-800 outline-none rounded-md" type="text" placeholder="Title" name="title">
                <textarea class="w-1/3 block px-5 py-2 bg-zinc-800 resize-none mt-2 outline-none rounded-md" placeholder="Add Task" name="details"></textarea>
                <input class="mt-4 bg-blue-600 px-4 py-2 rounded-full text-sm" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 text-white flex flex-wrap gap-3">
            <% if(files.length > 0) { %>
                <% files.forEach((val) => { %>
                    <div class="task w-72 px-4 py-4 rounded-md bg-zinc-800">
                        <h1 class=" text-xl tracking-tighter"><%= val %></h1>
                        <div class="flex w-full justify-between items-center mt-3">
                            <a class="text-white bg-blue-500 px-6 py-1 rounded-full inline-block" href="/file/<%= val %>">Read More >></a>
                            <a class="text-white bg-zinc-600 px-6 py-1 rounded-full" href="/edit/<%= val %>">Edit</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <h2 class="text-zinc-500">No Tasks Yet.</h2>
            <% } %>
        </div>
    </div>
</body>
</html>